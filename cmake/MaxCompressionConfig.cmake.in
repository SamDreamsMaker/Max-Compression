@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# MaxCompression optionally links OpenMP — find it if available
find_dependency(OpenMP QUIET)

include("${CMAKE_CURRENT_LIST_DIR}/MaxCompressionTargets.cmake")

check_required_components(MaxCompression)
