[package]
name = "maxcomp"
version = "1.0.0"
edition = "2021"
authors = ["MaxCompression <admin@maxcomp.local>"]
description = "Revolutionary file compression library bindings"

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1.0"
